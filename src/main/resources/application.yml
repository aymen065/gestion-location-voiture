
spring:
  datasource:
    url: jdbc:mysql://192.168.1.101/:3306/locationvoiture?createDatabaseIfNotExist=true&autoReconnect=true
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: true
    database: MYSQL
    show-sql: true
    hibernate: 
      ddl-auto: update
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
server:
  port: 8090
